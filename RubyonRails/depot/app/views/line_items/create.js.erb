if ($('#cart tr').length == 1) { $('#cart').slideDown('blind'); }

$("#cart").html("<%= escape_javascript render(@cart) %>");
//It tells the browser to replace the content of the element whose id="cart with that html"
$("#current_item").css({'background-color': '#88ff88'}).
	animate({'background-color': '#114411'}, 1000);